# Optional: URL to a backend proxy that forwards Claude requests with a server-side API key.
# When set, the browser will NEVER touch the Anthropic API key directly.
# Leave unset for local dev with the legacy "key in localStorage" mode.
VITE_AI_PROXY_URL=
